#!/bin/bash

pkg_name=$(paru -Slq | fzf --multi --preview 'paru -Sii {1}' --preview-window=down:75%)

if [[ -n "$pkg_name" ]]; then
  paru -Sy --noconfirm "$pkg_name"
  sudo updatedb
  ~/.local/share/omarchy/bin/omarchy-show-done
fi
